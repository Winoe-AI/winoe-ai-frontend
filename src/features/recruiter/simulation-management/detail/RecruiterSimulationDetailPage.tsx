import SimulationDetailContainer, {
  __testables,
} from './SimulationDetailContainer';

export default function RecruiterSimulationDetailPage() {
  return <SimulationDetailContainer />;
}

export { __testables };
