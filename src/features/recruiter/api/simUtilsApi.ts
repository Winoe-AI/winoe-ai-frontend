import {
  getId,
  getNumber,
  getString,
  isRecord,
  recruiterBffClient,
} from './baseApi';

export { getId, getNumber, getString, isRecord, recruiterBffClient };
export { toStringOrNull, toNumberOrNull, safeId } from './baseApi';
