# Flow-QA Scope

`tests/e2e/flow-qa` is mocked UI regression coverage.

- It is useful for fast component and flow verification.
- It is not contract proof.
- It must not be presented as real-stack evidence for recruiter, candidate, or evaluation completion.

Use `tests/e2e/contract-live` for Playwright runs that hit the real local stack.
